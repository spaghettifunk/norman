{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Commander",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/zig-out/bin/norman",
      "args": ["commander"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build"
    }
  ]
}
